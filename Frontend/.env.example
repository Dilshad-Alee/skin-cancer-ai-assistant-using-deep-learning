# Base URL of the FastAPI backend
# In Docker Compose the proxy handles /api and /static, so leave as empty string.
# For standalone dev pointing at a remote backend, set the full URL.
VITE_API_URL=http://localhost:8000
